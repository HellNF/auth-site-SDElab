"use client"

// Questo componente è un placeholder per eventuali popup personalizzati.
// Attualmente i popup/notification sono gestiti via Toaster (shadcn toast)
// e vengono attivati dal context in lib/oauth-events.tsx all'atto di addEvent.
// Se in futuro servisse un renderer dedicato, può essere implementato qui.

export function OAuthPopup() {
  return null
}
